spring:
  cloud:
    aws:
      region:
        static: eu-central-1
      credentials:
        access-key: foo
        secret-key: bar
      sqs:
        enabled: false
      s3:
        enabled: false
