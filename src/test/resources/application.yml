cloud:
  aws:
    region:
      static: eu-central-1
    credentials:
      access-key: foo
      secret-key: bar
    sns:
      enabled: false
    sqs:
      enabled: false
    stack:
      enabled: false
